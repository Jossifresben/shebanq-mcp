data/*.mql
data/*.sqlite3
__pycache__/
*.egg-info/
.pytest_cache/
.venv/

# Local secrets (never committed). The Zenodo token lives here.
.env
.env.*
*.token
.secrets/

# Planning artifacts (brainstorming specs + implementation plans) and project
# instructions — kept local, not in the public repo.
docs/specs/
docs/superpowers/
CLAUDE.md
